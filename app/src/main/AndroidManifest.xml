<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pharaohtech.kasralmakarxx">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".account.LoginActivity" />
        <activity android:name=".account.RegisterActivity" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="e26d9fd9f04eb94d2d11b001aa5f13ac48489501" />

        <activity android:name=".main.FullViewActivity" />
        <activity android:name=".account.ResetPasswordActivity" />
    </application>

</manifest>